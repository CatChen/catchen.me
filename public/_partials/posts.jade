h2
  | #{blogs.title}
ul
  for link in blogs.links
    li
      a(href="#{link.url}")
        | #{link.name}

h2
  | #{podcasts.title}
ul
  for link in podcasts.links
    li
      a(href="#{link.url}")
        | #{link.name}

if (typeof highlights !== 'undefined' && highlights !== null)
  h2
    | #{highlights.title}
  ul
    for link in highlights.links
      li
        if ('url' in link)
          a(href="#{link.url}")
            | #{link.name}
        else if ('urls' in link)
          | #{link.name}#{seriesStart}
          for url, index in link.urls
            | #{index > 0 ? seriesDelimiter : ''}
            a(href="#{url}")
              | #{index + 1}
          | #{seriesEnd}
